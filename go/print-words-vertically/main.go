package main

import (
	"fmt"
	"strings"
)

func main() {
	s := "HOW ARE YOU"
	fmt.Println(printVertically(s))
}

func printVertically(s string) []string {
	ss := strings.Split(s, " ")
	ans := []string{}
	for i, j := range ss {
		j[]
	}
}
